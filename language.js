
const LanguageData = {
    en: {
        name: 'English',
        code: 'en',
        flag: '🇺🇸',
        content: {
            // Navigation
            nav: {
                home: 'Home',
                explore: 'Explore',
                culture: 'Culture',
                ecoTrails: 'Eco Trails',
                rewards: 'Rewards',
                contact: 'Contact'
            },
            // Hero Section
            hero: {
                title: 'Discover <span class="highlight">Jharkhand\'s</span><br>Hidden Treasures',
                subtitle: 'Explore eco-friendly trails, rich tribal heritage, and breathtaking landscapes while earning rewards for responsible tourism',
                startExploring: 'Start Exploring',
                watchVideo: 'Watch Video',
                stats: {
                    ecoTrails: 'Eco Trails',
                    culturalSites: 'Cultural Sites',
                    localPartners: 'Local Partners'
                }
            },
            // Quick Access Cards
            quickAccess: {
                ecoTrails: {
                    title: 'Eco Trails',
                    description: 'Sustainable forest walks'
                },
                tribalHeritage: {
                    title: 'Tribal Heritage',
                    description: 'Rich cultural experiences'
                },
                wildlife: {
                    title: 'Wildlife',
                    description: 'Protected sanctuaries'
                },
                waterfalls: {
                    title: 'Waterfalls',
                    description: 'Pristine natural beauty'
                }
            },
            // Map Section
            map: {
                title: 'Interactive Explorer Map',
                loading: 'Loading Interactive Map...',
                filters: {
                    all: 'All',
                    eco: 'Eco Sites',
                    culture: 'Cultural',
                    wildlife: 'Wildlife',
                    waterfalls: 'Waterfalls'
                }
            },
            // Features
            features: {
                title: 'Platform Features',
                tripPlanner: {
                    title: 'Smart Trip Planner',
                    description: 'AI-powered eco-friendly travel planning with carbon footprint tracking'
                },
                gamification: {
                    title: 'Gamification Rewards',
                    description: 'Earn eco-badges and heritage tokens for responsible tourism practices'
                },
                homestays: {
                    title: 'Local Homestays',
                    description: 'Authentic tribal homestay experiences with certified local families'
                },
                guides: {
                    title: 'Community Guides',
                    description: 'Connect with local eco-guides and tribal culture experts'
                }
            },
            // Rewards
            rewards: {
                title: 'Your Eco Rewards',
                ecoPoints: 'Eco Points',
                badgesEarned: 'Badges Earned',
                carbonSaved: 'kg CO₂ Saved',
                availableBadges: 'Available Badges'
            },
            // Trip Planner
            tripPlanner: {
                title: 'Plan Your Eco Trip',
                travelDate: 'Travel Date',
                groupSize: 'Group Size',
                transport: 'Transport',
                accommodation: 'Accommodation',
                summary: 'Trip Summary',
                carbonFootprint: 'Carbon Footprint',
                confirmTrip: 'Confirm Eco Trip',
                groupSizes: {
                    solo: 'Solo Traveler',
                    couple: 'Couple',
                    small: 'Small Group (3-5)',
                    large: 'Large Group (6+)'
                },
                transportOptions: {
                    bus: {
                        name: 'Eco Bus',
                        description: 'Low carbon footprint'
                    },
                    train: {
                        name: 'Train',
                        description: 'Eco-friendly option'
                    },
                    car: {
                        name: 'Shared Car',
                        description: 'Moderate impact'
                    }
                },
                accommodationOptions: {
                    homestay: {
                        name: 'Tribal Homestay',
                        description: 'Support local community'
                    },
                    ecoLodge: {
                        name: 'Eco Lodge',
                        description: 'Sustainable accommodation'
                    }
                }
            },
            // Notifications
            notifications: {
                tripConfirmed: 'Trip confirmed! You earned {points} eco points!',
                completeDetails: 'Please complete all trip details',
                pointsEarned: '+{points} points earned!'
            },
            // Footer
            footer: {
                description: 'Smart digital platform for sustainable eco and cultural tourism',
                quickLinks: 'Quick Links',
                support: 'Support',
                helpCenter: 'Help Center',
                contactUs: 'Contact Us',
                feedback: 'Feedback',
                terms: 'Terms of Service',
                copyright: '© 2025 CODE FORGE. All rights reserved. | Smart India Hackathon Project'
            }
        }
    },
    hi: {
        name: 'हिन्दी',
        code: 'hi',
        flag: '🇮🇳',
        content: {
            // Navigation
            nav: {
                home: 'होम',
                explore: 'अन्वेषण करें',
                culture: 'संस्कृति',
                ecoTrails: 'इको ट्रेल्स',
                rewards: 'पुरस्कार',
                contact: 'संपर्क'
            },
            // Hero Section
            hero: {
                title: '<span class="highlight">झारखंड के</span><br>छिपे हुए खजाने की खोज करें',
                subtitle: 'पर्यावरण-अनुकूल पगडंडियों, समृद्ध आदिवासी विरासत और मनमोहक प्राकृतिक दृश्यों का अन्वेषण करें और जिम्मेदार पर्यटन के लिए पुरस्कार अर्जित करें',
                startExploring: 'अन्वेषण शुरू करें',
                watchVideo: 'वीडियो देखें',
                stats: {
                    ecoTrails: 'इको ट्रेल्स',
                    culturalSites: 'सांस्कृतिक स्थल',
                    localPartners: 'स्थानीय साझेदार'
                }
            },
            // Quick Access Cards
            quickAccess: {
                ecoTrails: {
                    title: 'इको ट्रेल्स',
                    description: 'टिकाऊ वन पदयात्राएं'
                },
                tribalHeritage: {
                    title: 'आदिवासी विरासत',
                    description: 'समृद्ध सांस्कृतिक अनुभव'
                },
                wildlife: {
                    title: 'वन्यजीव',
                    description: 'संरक्षित अभयारण्य'
                },
                waterfalls: {
                    title: 'जलप्रपात',
                    description: 'प्राकृतिक सौंदर्य'
                }
            },
            // Map Section
            map: {
                title: 'इंटरैक्टिव एक्सप्लोरर मैप',
                loading: 'इंटरैक्टिव मैप लोड हो रहा है...',
                filters: {
                    all: 'सभी',
                    eco: 'इको साइटें',
                    culture: 'सांस्कृतिक',
                    wildlife: 'वन्यजीव',
                    waterfalls: 'जलप्रपात'
                }
            },
            // Features
            features: {
                title: 'प्लेटफॉर्म सुविधाएं',
                tripPlanner: {
                    title: 'स्मार्ट ट्रिप प्लानर',
                    description: 'कार्बन फुटप्रिंट ट्रैकिंग के साथ AI-संचालित पर्यावरण-अनुकूल यात्रा योजना'
                },
                gamification: {
                    title: 'गेमिफिकेशन रिवार्ड्स',
                    description: 'जिम्मेदार पर्यटन प्रथाओं के लिए इको-बैज और विरासत टोकन अर्जित करें'
                },
                homestays: {
                    title: 'स्थानीय होमस्टे',
                    description: 'प्रमाणित स्थानीय परिवारों के साथ प्रामाणिक आदिवासी होमस्टे अनुभव'
                },
                guides: {
                    title: 'सामुदायिक गाइड',
                    description: 'स्थानीय इको-गाइडों और आदिवासी संस्कृति विशेषज्ञों से जुड़ें'
                }
            },
            // Rewards
            rewards: {
                title: 'आपके इको रिवार्ड्स',
                ecoPoints: 'इको अंक',
                badgesEarned: 'अर्जित बैज',
                carbonSaved: 'kg CO₂ बचाया गया',
                availableBadges: 'उपलब्ध बैज'
            },
            // Trip Planner
            tripPlanner: {
                title: 'अपनी इको ट्रिप की योजना बनाएं',
                travelDate: 'यात्रा तिथि',
                groupSize: 'समूह का आकार',
                transport: 'परिवहन',
                accommodation: 'आवास',
                summary: 'यात्रा सारांश',
                carbonFootprint: 'कार्बन फुटप्रिंट',
                confirmTrip: 'इको ट्रिप की पुष्टि करें',
                groupSizes: {
                    solo: 'अकेले यात्री',
                    couple: 'जोड़ा',
                    small: 'छोटा समूह (3-5)',
                    large: 'बड़ा समूह (6+)'
                },
                transportOptions: {
                    bus: {
                        name: 'इको बस',
                        description: 'कम कार्बन फुटप्रिंट'
                    },
                    train: {
                        name: 'ट्रेन',
                        description: 'पर्यावरण-अनुकूल विकल्प'
                    },
                    car: {
                        name: 'शेयर्ड कार',
                        description: 'मध्यम प्रभाव'
                    }
                },
                accommodationOptions: {
                    homestay: {
                        name: 'आदिवासी होमस्टे',
                        description: 'स्थानीय समुदाय का समर्थन करें'
                    },
                    ecoLodge: {
                        name: 'इको लॉज',
                        description: 'टिकाऊ आवास'
                    }
                }
            },
            // Notifications
            notifications: {
                tripConfirmed: 'यात्रा की पुष्टि हो गई! आपने {points} इको अंक अर्जित किए!',
                completeDetails: 'कृपया सभी यात्रा विवरण पूरे करें',
                pointsEarned: '+{points} अंक अर्जित!'
            },
            // Footer
            footer: {
                description: 'टिकाऊ इको और सांस्कृतिक पर्यटन के लिए स्मार्ट डिजिटल प्लेटफॉर्म',
                quickLinks: 'त्वरित लिंक',
                support: 'सहायता',
                helpCenter: 'सहायता केंद्र',
                contactUs: 'संपर्क करें',
                feedback: 'प्रतिक्रिया',
                terms: 'सेवा की शर्तें',
                copyright: '© 2025 कोड फोर्ज। सभी अधिकार सुरक्षित। | स्मार्ट इंडिया हैकाथॉन प्रोजेक्ट'
            }
        }
    },
    bho: {
        name: 'भोजपुरी',
        code: 'bho',
        flag: '🏴‍☠️',
        content: {
            // Navigation
            nav: {
                home: 'घर',
                explore: 'खोजीं',
                culture: 'संस्कार',
                ecoTrails: 'प्रकृति रस्ता',
                rewards: 'इनाम',
                contact: 'संपर्क'
            },
            // Hero Section
            hero: {
                title: '<span class="highlight">झारखंड के</span><br>छुपल खजाना खोजीं',
                subtitle: 'प्रकृति के अनुकूल रस्ता, आदिवासी संस्कार आ प्राकृतिक सुंदरता के खोज करीं आ जिम्मेदार घुमइया के लिए इनाम पाईं',
                startExploring: 'खोज शुरू करीं',
                watchVideo: 'वीडियो देखीं',
                stats: {
                    ecoTrails: 'प्रकृति रस्ता',
                    culturalSites: 'संस्कृति जगह',
                    localPartners: 'स्थानीय साझीदार'
                }
            },
            // Quick Access Cards
            quickAccess: {
                ecoTrails: {
                    title: 'प्रकृति रस्ता',
                    description: 'जंगल में टिकाऊ घुमइया'
                },
                tribalHeritage: {
                    title: 'आदिवासी विरासत',
                    description: 'समृद्ध सांस्कृतिक अनुभव'
                },
                wildlife: {
                    title: 'जंगली जानवर',
                    description: 'संरक्षित जगह'
                },
                waterfalls: {
                    title: 'झरना',
                    description: 'प्राकृतिक सुंदरता'
                }
            },
            // Map Section
            map: {
                title: 'इंटरैक्टिव एक्सप्लोरर नक्शा',
                loading: 'नक्शा लोड हो रहल बा...',
                filters: {
                    all: 'सब',
                    eco: 'प्रकृति जगह',
                    culture: 'संस्कृति',
                    wildlife: 'जंगली जानवर',
                    waterfalls: 'झरना'
                }
            },
            // Features
            features: {
                title: 'प्लेटफॉर्म सुविधा',
                tripPlanner: {
                    title: 'स्मार्ट ट्रिप प्लानर',
                    description: 'कार्बन फुटप्रिंट ट्रैकिंग के साथ AI से चलल प्रकृति-अनुकूल यात्रा योजना'
                },
                gamification: {
                    title: 'गेम जइसन इनाम',
                    description: 'जिम्मेदार घुमइया के लिए इको-बैज आ विरासत टोकन कमाईं'
                },
                homestays: {
                    title: 'स्थानीय होमस्टे',
                    description: 'प्रमाणित स्थानीय परिवार के साथ असली आदिवासी होमस्टे अनुभव'
                },
                guides: {
                    title: 'समुदायिक गाइड',
                    description: 'स्थानीय प्रकृति-गाइड आ आदिवासी संस्कृति के जानकार से जुड़ीं'
                }
            },
            // Rewards
            rewards: {
                title: 'राउर इको इनाम',
                ecoPoints: 'प्रकृति अंक',
                badgesEarned: 'मिलल बैज',
                carbonSaved: 'kg CO₂ बचावल',
                availableBadges: 'उपलब्ध बैज'
            },
            // Trip Planner
            tripPlanner: {
                title: 'आपन प्रकृति यात्रा के योजना बनाईं',
                travelDate: 'यात्रा के तारीख',
                groupSize: 'समूह के साइज',
                transport: 'परिवहन',
                accommodation: 'रुकइया जगह',
                summary: 'यात्रा सारांश',
                carbonFootprint: 'कार्बन फुटप्रिंट',
                confirmTrip: 'प्रकृति यात्रा के पुष्टि करीं',
                groupSizes: {
                    solo: 'अकेले यात्री',
                    couple: 'जोड़ा',
                    small: 'छोट समूह (3-5)',
                    large: 'बड़ समूह (6+)'
                },
                transportOptions: {
                    bus: {
                        name: 'प्रकृति बस',
                        description: 'कम कार्बन फुटप्रिंट'
                    },
                    train: {
                        name: 'रेल गाड़ी',
                        description: 'प्रकृति-अनुकूल विकल्प'
                    },
                    car: {
                        name: 'साझा कार',
                        description: 'मध्यम प्रभाव'
                    }
                },
                accommodationOptions: {
                    homestay: {
                        name: 'आदिवासी होमस्टे',
                        description: 'स्थानीय समुदाय के सहारा करीं'
                    },
                    ecoLodge: {
                        name: 'प्रकृति लॉज',
                        description: 'टिकाऊ रुकइया जगह'
                    }
                }
            },
            // Notifications
            notifications: {
                tripConfirmed: 'यात्रा के पुष्टि हो गइल! राउर {points} प्रकृति अंक मिलल!',
                completeDetails: 'कृपा करके सब यात्रा विवरण पूरा करीं',
                pointsEarned: '+{points} अंक मिलल!'
            },
            // Footer
            footer: {
                description: 'टिकाऊ प्रकृति आ सांस्कृतिक पर्यटन के लिए स्मार्ट डिजिटल प्लेटफॉर्म',
                quickLinks: 'झटपट लिंक',
                support: 'सहारा',
                helpCenter: 'मदद केंद्र',
                contactUs: 'संपर्क करीं',
                feedback: 'राय',
                terms: 'सेवा के शर्त',
                copyright: '© 2025 कोड फोर्ज। सब अधिकार सुरक्षित। | स्मार्ट इंडिया हैकाथॉन प्रोजेक्ट'
            }
        }
    }
};

// Current language state
let currentLanguage = 'en';

// Initialize language support
function initializeLanguageSupport() {
    // Load saved language preference
    const savedLanguage = localStorage.getItem('jharkhandExplorerLanguage') || 'en';
    setLanguage(savedLanguage);
    
    // Set up language switcher
    setupLanguageSwitcher();
    
    console.log('🌐 Language support initialized');
}

// Set up language switcher functionality
function setupLanguageSwitcher() {
    const langSwitcher = document.getElementById('langSwitcher');
    if (!langSwitcher) return;
    
    // Create language dropdown
    createLanguageDropdown();
    
    // Add click event to toggle dropdown
    langSwitcher.addEventListener('click', (e) => {
        e.stopPropagation();
        toggleLanguageDropdown();
    });
    
    // Close dropdown when clicking outside
    document.addEventListener('click', (e) => {
        if (!langSwitcher.contains(e.target)) {
            closeLanguageDropdown();
        }
    });
}

// Create language dropdown
function createLanguageDropdown() {
    const langSwitcher = document.getElementById('langSwitcher');
    const dropdown = document.createElement('div');
    dropdown.className = 'language-selector';
    dropdown.id = 'languageDropdown';
    
    // Create options for each language
    Object.values(LanguageData).forEach(lang => {
        const option = document.createElement('div');
        option.className = `language-option ${lang.code === currentLanguage ? 'active' : ''}`;
        option.innerHTML = `
            <span>${lang.flag}</span>
            <span>${lang.name}</span>
        `;
        option.addEventListener('click', () => {
            setLanguage(lang.code);
            closeLanguageDropdown();
        });
        dropdown.appendChild(option);
    });
    
    langSwitcher.appendChild(dropdown);
}

// Toggle language dropdown
function toggleLanguageDropdown() {
    const dropdown = document.getElementById('languageDropdown');
    if (dropdown) {
        dropdown.classList.toggle('show');
    }
}

// Close language dropdown
function closeLanguageDropdown() {
    const dropdown = document.getElementById('languageDropdown');
    if (dropdown) {
        dropdown.classList.remove('show');
    }
}

// Set language and update UI
function setLanguage(langCode) {
    if (!LanguageData[langCode]) {
        console.warn(`Language ${langCode} not found, defaulting to English`);
        langCode = 'en';
    }
    
    currentLanguage = langCode;
    const langData = LanguageData[langCode];
    
    // Update language switcher button
    updateLanguageSwitcherButton(langData);
    
    // Update all text content
    updatePageContent(langData.content);
    
    // Update active option in dropdown
    updateLanguageDropdownActive(langCode);
    
    // Save preference
    localStorage.setItem('jharkhandExplorerLanguage', langCode);
    
    // Update document language attribute
    document.documentElement.lang = langCode;
    
    console.log(`🌐 Language changed to: ${langData.name}`);
}

// Update language switcher button
function updateLanguageSwitcherButton(langData) {
    const langSwitcher = document.getElementById('langSwitcher');
    if (langSwitcher) {
        langSwitcher.innerHTML = `<i class="fas fa-globe"></i> ${langData.code.toUpperCase()}`;
    }
}

// Update language dropdown active state
function updateLanguageDropdownActive(langCode) {
    const options = document.querySelectorAll('.language-option');
    options.forEach(option => {
        option.classList.remove('active');
        if (option.innerHTML.includes(LanguageData[langCode].name)) {
            option.classList.add('active');
        }
    });
}

// Update page content with new language
function updatePageContent(content) {
    // Update navigation
    updateNavigation(content.nav);
    
    // Update hero section
    updateHeroSection(content.hero);
    
    // Update quick access cards
    updateQuickAccessCards(content.quickAccess);
    
    // Update map section
    updateMapSection(content.map);
    
    // Update features section
    updateFeaturesSection(content.features);
    
    // Update rewards section
    updateRewardsSection(content.rewards);
    
    // Update footer
    updateFooter(content.footer);
    
    // Update any existing modals
    updateModalsContent(content);
}

// Update navigation text
function updateNavigation(navContent) {
    const navLinks = document.querySelectorAll('.nav-link');
    const navMap = {
        '#home': navContent.home,
        '#explore': navContent.explore,
        '#culture': navContent.culture,
        '#eco-trails': navContent.ecoTrails,
        '#rewards': navContent.rewards,
        '#contact': navContent.contact
    };
    
    navLinks.forEach(link => {
        const href = link.getAttribute('href');
        if (navMap[href]) {
            link.textContent = navMap[href];
        }
    });
}

// Update hero section
function updateHeroSection(heroContent) {
    const heroTitle = document.querySelector('.hero-title');
    const heroSubtitle = document.querySelector('.hero-subtitle');
    const startExploringBtn = document.getElementById('startExploring');
    const watchVideoBtn = document.getElementById('watchVideo');
    
    if (heroTitle) {
        heroTitle.innerHTML = heroContent.title;
    }
    
    if (heroSubtitle) {
        heroSubtitle.textContent = heroContent.subtitle;
    }
    
    if (startExploringBtn) {
        startExploringBtn.innerHTML = `<i class="fas fa-compass"></i> ${heroContent.startExploring}`;
    }
    
    if (watchVideoBtn) {
        watchVideoBtn.innerHTML = `<i class="fas fa-play"></i> ${heroContent.watchVideo}`;
    }
    
    // Update stats
    const statItems = document.querySelectorAll('.stat-item p');
    const statTexts = [
        heroContent.stats.ecoTrails,
        heroContent.stats.culturalSites,
        heroContent.stats.localPartners
    ];
    
    statItems.forEach((item, index) => {
        if (statTexts[index]) {
            item.textContent = statTexts[index];
        }
    });
}

// Update quick access cards
function updateQuickAccessCards(quickAccessContent) {
    const cards = document.querySelectorAll('.quick-card');
    const cardData = [
        quickAccessContent.ecoTrails,
        quickAccessContent.tribalHeritage,
        quickAccessContent.wildlife,
        quickAccessContent.waterfalls
    ];
    
    cards.forEach((card, index) => {
        if (cardData[index]) {
            const title = card.querySelector('h3');
            const description = card.querySelector('p');
            
            if (title) title.textContent = cardData[index].title;
            if (description) description.textContent = cardData[index].description;
        }
    });
}

// Update map section
function updateMapSection(mapContent) {
    const mapTitle = document.querySelector('#explore .section-title');
    const mapLoading = document.querySelector('.map-loading p');
    const mapButtons = document.querySelectorAll('.map-btn');
    
    if (mapTitle) {
        mapTitle.textContent = mapContent.title;
    }
    
    if (mapLoading) {
        mapLoading.textContent = mapContent.loading;
    }
    
    // Update map filter buttons
    const filterMap = {
        'all': mapContent.filters.all,
        'eco': mapContent.filters.eco,
        'culture': mapContent.filters.culture,
        'wildlife': mapContent.filters.wildlife,
        'waterfalls': mapContent.filters.waterfalls
    };
    
    mapButtons.forEach(btn => {
        const filter = btn.getAttribute('data-filter');
        if (filterMap[filter]) {
            btn.textContent = filterMap[filter];
        }
    });
}

// Update features section
function updateFeaturesSection(featuresContent) {
    const featuresTitle = document.querySelector('.features-section .section-title');
    const featureCards = document.querySelectorAll('.feature-card');
    
    if (featuresTitle) {
        featuresTitle.textContent = featuresContent.title;
    }
    
    const featureData = [
        featuresContent.tripPlanner,
        featuresContent.gamification,
        featuresContent.homestays,
        featuresContent.guides
    ];
    
    featureCards.forEach((card, index) => {
        if (featureData[index]) {
            const title = card.querySelector('h3');
            const description = card.querySelector('p');
            
            if (title) title.textContent = featureData[index].title;
            if (description) description.textContent = featureData[index].description;
        }
    });
}

// Update rewards section
function updateRewardsSection(rewardsContent) {
    const rewardsTitle = document.querySelector('.rewards-section .section-title');
    const badgesTitle = document.querySelector('.badges-showcase h4');
    
    if (rewardsTitle) {
        rewardsTitle.textContent = rewardsContent.title;
    }
    
    if (badgesTitle) {
        badgesTitle.textContent = rewardsContent.availableBadges;
    }
    
    // Update reward stat labels
    const rewardStats = document.querySelectorAll('.reward-stat p');
    const statLabels = [
        rewardsContent.ecoPoints,
        rewardsContent.badgesEarned,
        rewardsContent.carbonSaved
    ];
    
    rewardStats.forEach((stat, index) => {
        if (statLabels[index]) {
            stat.textContent = statLabels[index];
        }
    });
}

// Update footer
function updateFooter(footerContent) {
    const footerDescription = document.querySelector('.footer-section p');
    const footerHeadings = document.querySelectorAll('.footer-section h4');
    const footerLinks = document.querySelectorAll('.footer-section ul li a');
    const copyrightText = document.querySelector('.footer-bottom p');
    
    if (footerDescription) {
        footerDescription.textContent = footerContent.description;
    }
    
    // Update section headings
    const headings = [
        'Jharkhand Explorer',
        footerContent.quickLinks,
        footerContent.support,
        'CODE FORGE'
    ];
    
    footerHeadings.forEach((heading, index) => {
        if (headings[index] && index !== 0 && index !== 3) { // Skip brand name and team name
            heading.textContent = headings[index];
        }
    });
    
    // Update footer links
    const linkTexts = [
        footerContent.quickLinks,
        footerContent.helpCenter,
        footerContent.contactUs,
        footerContent.feedback,
        footerContent.terms
    ];
    
    // This would need more specific selectors based on actual footer structure
    if (copyrightText) {
        copyrightText.textContent = footerContent.copyright;
    }
}

// Update existing modals with new language
function updateModalsContent(content) {
    // Update trip planner modal if exists
    const tripPlannerModal = document.getElementById('tripPlanner');
    if (tripPlannerModal && tripPlannerModal.style.display !== 'none') {
        updateTripPlannerModal(content.tripPlanner);
    }
}

// Update trip planner modal content
function updateTripPlannerModal(tripPlannerContent) {
    const modal = document.getElementById('tripPlanner');
    if (!modal) return;
    
    // Update modal title
    const modalTitle = modal.querySelector('.modal-title');
    if (modalTitle) {
        modalTitle.textContent = tripPlannerContent.title;
    }
    
    // Update option group headings
    const optionGroups = modal.querySelectorAll('.option-group h4');
    const groupLabels = [
        tripPlannerContent.travelDate,
        tripPlannerContent.groupSize,
        tripPlannerContent.transport,
        tripPlannerContent.accommodation
    ];
    
    optionGroups.forEach((group, index) => {
        if (groupLabels[index]) {
            const icon = group.querySelector('i');
            const iconHTML = icon ? icon.outerHTML : '';
            group.innerHTML = iconHTML + ' ' + groupLabels[index];
        }
    });
    
    // Update group size options
    const groupSizeSelect = modal.querySelector('#groupSize');
    if (groupSizeSelect) {
        const options = groupSizeSelect.querySelectorAll('option');
        const optionTexts = [
            tripPlannerContent.groupSizes.solo,
            tripPlannerContent.groupSizes.couple,
            tripPlannerContent.groupSizes.small,
            tripPlannerContent.groupSizes.large
        ];
        
        options.forEach((option, index) => {
            if (optionTexts[index]) {
                option.textContent = optionTexts[index];
            }
        });
    }
    
    // Update transport options
    updateTransportOptions(modal, tripPlannerContent.transportOptions);
    
    // Update accommodation options
    updateAccommodationOptions(modal, tripPlannerContent.accommodationOptions);
    
    // Update summary section
    const summaryTitle = modal.querySelector('.trip-summary h4');
    const carbonLabel = modal.querySelector('.carbon-footprint span');
    const pointsLabel = modal.querySelector('.eco-points span');
    const confirmButton = modal.querySelector('.trip-summary .btn');
    
    if (summaryTitle) summaryTitle.textContent = tripPlannerContent.summary;
    if (carbonLabel) carbonLabel.innerHTML = `${tripPlannerContent.carbonFootprint}: <strong id="carbonFootprint">0 kg CO₂</strong>`;
    if (pointsLabel) pointsLabel.innerHTML = `${tripPlannerContent.ecoPoints}: <strong id="tripPoints">0</strong>`;
    if (confirmButton) confirmButton.innerHTML = `<i class="fas fa-check"></i> ${tripPlannerContent.confirmTrip}`;
}

// Update transport options in modal
function updateTransportOptions(modal, transportOptions) {
    const transportLabels = modal.querySelectorAll('.transport-option');
    const optionData = [
        transportOptions.bus,
        transportOptions.train,
        transportOptions.car
    ];
    
    transportLabels.forEach((label, index) => {
        if (optionData[index]) {
            const span = label.querySelector('span');
            const small = label.querySelector('small');
            const icon = span ? span.querySelector('i') : null;
            
            if (span && icon) {
                span.innerHTML = icon.outerHTML + ' ' + optionData[index].name;
            }
            if (small) {
                small.textContent = optionData[index].description;
            }
        }
    });
}

// Update accommodation options in modal
function updateAccommodationOptions(modal, accommodationOptions) {
    const accommodationLabels = modal.querySelectorAll('.accommodation-option');
    const optionData = [
        accommodationOptions.homestay,
        accommodationOptions.ecoLodge
    ];
    
    accommodationLabels.forEach((label, index) => {
        if (optionData[index]) {
            const span = label.querySelector('span');
            const small = label.querySelector('small');
            const icon = span ? span.querySelector('i') : null;
            
            if (span && icon) {
                span.innerHTML = icon.outerHTML + ' ' + optionData[index].name;
            }
            if (small) {
                small.textContent = optionData[index].description;
            }
        }
    });
}

// Get translated text
function getTranslatedText(key, replacements = {}) {
    const keys = key.split('.');
    let text = LanguageData[currentLanguage].content;
    
    // Navigate through nested object
    for (const k of keys) {
        if (text && typeof text === 'object' && text[k]) {
            text = text[k];
        } else {
            // Fallback to English if key not found
            text = LanguageData.en.content;
            for (const k2 of keys) {
                if (text && typeof text === 'object' && text[k2]) {
                    text = text[k2];
                } else {
                    return key; // Return key if not found in any language
                }
            }
            break;
        }
    }
    
    // Replace placeholders
    if (typeof text === 'string') {
        Object.keys(replacements).forEach(placeholder => {
            text = text.replace(`{${placeholder}}`, replacements[placeholder]);
        });
    }
    
    return text;
}

// Show notification in current language
function showLocalizedNotification(messageKey, type = 'info', replacements = {}) {
    const message = getTranslatedText(`notifications.${messageKey}`, replacements);
    showNotification(message, type);
}

// Get current language code
function getCurrentLanguage() {
    return currentLanguage;
}

// Get current language data
function getCurrentLanguageData() {
    return LanguageData[currentLanguage];
}

// Check if RTL language (for future Arabic/Urdu support)
function isRTLLanguage(langCode = currentLanguage) {
    const rtlLanguages = ['ar', 'ur', 'fa']; // Arabic, Urdu, Persian
    return rtlLanguages.includes(langCode);
}

// Export functions for global use
window.LanguageSupport = {
    initialize: initializeLanguageSupport,
    setLanguage: setLanguage,
    getCurrentLanguage: getCurrentLanguage,
    getCurrentLanguageData: getCurrentLanguageData,
    getTranslatedText: getTranslatedText,
    showLocalizedNotification: showLocalizedNotification,
    isRTLLanguage: isRTLLanguage
};